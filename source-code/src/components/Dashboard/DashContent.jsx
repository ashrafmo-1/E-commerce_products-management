import React from 'react';
import Container from "react-bootstrap/Container";

const DashContent = () => {
    return (
        <Container className={'mt-4'}>
            <h1 className={'text-center'}>welcome to dashboard management</h1>
            <p className={'text-center'}> can be management of the dashboard and manage all products, users add new user, show all products and users </p>
        </Container>
    )
}

export default DashContent;